{
  "name": "microapp",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "scripts": {
    "bootstrap": "yarn --cwd account && yarn --cwd cards && yarn --cwd host",
    "pod-install": "cd account/ios/ && pod install && cd ../../cards/ios/ && pod install && cd ../../host/ios && pod install",
    "start:account": "yarn --cwd account start",
    "start:cards": "yarn --cwd cards start",
    "start:host": "yarn --cwd host start",
    "run:host:ios": "yarn --cwd host ios",
    "run:host:android": "yarn --cwd host android",
    "adbreverse": "adb reverse tcp:3000 tcp:3000 && adb reverse tcp:8081 tcp:8081 && adb reverse tcp:8083 tcp:8083 && adb reverse tcp:8084 tcp:8084",
    "start": "concurrently -P 'yarn:start:*(!standalone) {1}'"

  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
